import { indexMatch } from 'bandmates/helpers/index-match';
import { module, test } from 'qunit';

module('Unit | Helper | index match');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = indexMatch([42]);
  assert.ok(result);
});
