import { currentUserCheck } from 'bandmates/helpers/current-user-check';
import { module, test } from 'qunit';

module('Unit | Helper | current user check');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currentUserCheck([42]);
  assert.ok(result);
});
